// src/pages/PitchingMLBBenchmarks.js
import React from 'react';

export default function PitchingMLBBenchmarks() {
  return (
    <div
      style={{
        minHeight: '100vh',
        padding: '32px 24px',
        background: 'linear-gradient(180deg, #0d1b2a 0%, #0b2d4d 100%)',
      }}
    >
      <h1
        style={{
          color: '#FFD166',           // gold/yellow
          margin: '0 0 8px 0',
          fontSize: '36px',
          fontWeight: 900,
          letterSpacing: '0.5px',
        }}
      >
        MLB Benchmarks
      </h1>

      <p style={{ color: '#c7d3e3', margin: 0 }}>
        Placeholder — pitching benchmarks page (content coming soon).
      </p>
    </div>
  );
}
